import { BrowserDynamicTestingModule } from "@angular/platform-browser-dynamic/testing";

export class Event {
    id: number;
    title: string;
    location: string;
    body: string;
    eventDate: string;
  }